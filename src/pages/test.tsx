import styled from 'styled-components';

function Test() {
  return <Root>{/* <RecipeCard cardInfo={} /> */}</Root>;
}

export default Test;

const Root = styled.main`
  display: flex;
  flex-direction: column;
`;
